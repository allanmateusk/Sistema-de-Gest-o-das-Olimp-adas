# Ver https://micronaut-projects.github.io/micronaut-openapi/ — views desativadas por predefinição; sem isto, /swagger-ui fica vazio ou 404.
swagger-ui.enabled=true
redoc.enabled=false
rapidoc.enabled=false
